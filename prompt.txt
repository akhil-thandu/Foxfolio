Run the seed script against the production Neon database.

cd C:/Logan/Projects/Foxfolio/backend && python -c "
import os
os.environ['DATABASE_URL'] = 'postgresql://neondb_owner:npg_uqtl4BG5fXvy@ep-curly-snow-a4sin8zm-pooler.us-east-1.aws.neon.tech/foxfolio_prod?sslmode=require&channel_binding=require'

import uuid
from sqlalchemy import create_engine
from sqlalchemy.orm import Session
from app.db.models import User, Account, Settings
from app.core.security import hash_password

engine = create_engine(os.environ['DATABASE_URL'])
db = Session(engine)

existing = db.query(User).first()
if existing:
    print('Already seeded')
else:
    user = User(id=uuid.uuid4(), username='logan', password_hash=hash_password('logandev123'))
    db.add(user)
    db.flush()
    for name, atype, currency in [('Groww','indian_broker','INR'),('Robinhood','robinhood','USD'),('Crypto','crypto','USD')]:
        db.add(Account(id=uuid.uuid4(), user_id=user.id, name=name, account_type=atype, currency=currency))
    db.add(Settings(id=uuid.uuid4(), user_id=user.id, base_currency='USD', price_refresh_interval=1))
    db.commit()
    print('Seeded production database successfully')
    print(f'User: logan | Accounts: Groww, Robinhood, Crypto')
db.close()
"